{
  "name": "pp-agent",
  "version": "1.0.0",
  "description": "ProSendWorship ProPresenter Bridge Agent",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "setup": "node setup.js",
    "build": "pkg agent.js --config pkg.config.json --target node18-win-x64 --output dist/pp-agent.exe"
  },
  "dependencies": {
    "protobufjs": "^7.4.0"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^5.12.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
